# PII 5종 패턴 명세

AI가 서비스 코드 작성 시 엄격히 금지하는 5가지 개인식별정보(PII) 패턴입니다.

## 1. 주민등록번호 (RRN)

**패턴 설명:**
- 형식: `YYMMDD-NNNNNNN` (13자리, 하이픈 포함) 또는 `YYMMDDNNNNNNN` (13자리 연속)
- 예: `900101-1234567`, `9001011234567`

**정규식:**
```regex
\b\d{2}(0[1-9]|1[0-2])(0[1-9]|[12]\d|3[01])-?[1-4]\d{6}\b
```

**거절 anchor:**
https://github.com/OPEN-Healthcare/ohc-changemgmt-hub-rules/blob/main/pii-patterns.md#L11

**금지 케이스:**
- 주민등록번호를 평문 저장, 로깅, 또는 API 응답에 포함
- 주민등록번호 기반 해시 생성 (해시값도 추적 가능)
- 고객 인증/검증을 위해 주민등록번호 검증 로직 작성

**우회 방법:**
- DB 레이어: 암호화 저장 필수 (AES-256-GCM, 환자별 키 rotation)
- API: 마스킹 반환 (예: `900101-****567`)
- 검증: 외부 서비스(KIS/정부24 API) 위임, 결과만 boolean 저장

---

## 2. 전화번호 (Phone Number)

**패턴 설명:**
- 형식: `010-1234-5678`, `02-1234-5678`, `031-1234-5678` 등
- 예: `010-1234-5678`, `02123456789` (하이픈 생략)

**정규식:**
```regex
\b(010|02|031|032|033|041|042|051|052|053|054|055|061|062|063|064)\d{3,4}\d{4}\b
```

**거절 anchor:**
https://github.com/OPEN-Healthcare/ohc-changemgmt-hub-rules/blob/main/pii-patterns.md#L33

**금지 케이스:**
- 전화번호를 평문으로 API 응답/로그 출력
- SMS/카톡 전송 기록을 평문 저장
- 전화번호 기반 사용자 추적/프로파일링

**우회 방법:**
- DB 레이어: 암호화 저장 + 해시 인덱스 (검색용)
- API: 마스킹 반환 (예: `010-****-5678`)
- 발송: 외부 SMS 서비스 위임, 발송 기록만 암호화 저장

---

## 3. 이메일 주소 (Email)

**패턴 설명:**
- 형식: `user@example.com`, `patient+tag@clinic.co.kr`

**정규식:**
```regex
\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Z|a-z]{2,}\b
```

**거절 anchor:**
https://github.com/OPEN-Healthcare/ohc-changemgmt-hub-rules/blob/main/pii-patterns.md#L53

**금지 케이스:**
- 이메일을 평문 로그 출력
- 이메일 기반 사용자 추적/마케팅 리스트 자동 생성
- 가입자 이메일 리스트를 API 응답에 노출

**우회 방법:**
- DB 레이어: 해시 인덱스만 저장 (검색용), 평문은 암호화
- API: 마스킹 반환 (예: `us****@example.com`)
- 발송: 외부 이메일 서비스 위임, 발송 기록만 로깅

---

## 4. 환자명 (Patient Name)

**패턴 설명:**
- 한글 이름, 외국 이름 모두 포함
- 예: `김철수`, `John Smith`, `이영희`

**검출 휴리스틱:**
```
- 2~4글자 한글 문자
- 외국인 이름(CJK 제외 알파벳 연속)
- 맥락: "환자: ...", "이름: ..." 등 주변 필드
```

**거절 anchor:**
https://github.com/OPEN-Healthcare/ohc-changemgmt-hub-rules/blob/main/pii-patterns.md#L73

**금지 케이스:**
- 환자명을 로그/API 응답에 평문 출력
- 환자명 기반 검색 가능하도록 노출 (예: 자동완성)
- 진료 기록과 환자명을 연결하는 API

**우회 방법:**
- DB 레이어: 해시 인덱스 + 암호화 저장
- API: 마스킹 반환 (예: `김**`, `Jo**`)
- 검색: 의료진 본인/팀원만 접근 가능하도록 권한 검증

---

## 5. 진료기록 ID (Medical Record ID / Chart Number)

**패턴 설명:**
- 형식: `CHR-2026-000001`, `PT-12345`, `MRN-ABC123` 등
- 환자 의료 기록을 추적하는 고유 식별자

**검출 휴리스틱:**
```
- 접두사(CHR/PT/MRN) + 숫자 조합
- 맥락: "의료 기록", "차트", "진료 ID" 등
```

**거절 anchor:**
https://github.com/OPEN-Healthcare/ohc-changemgmt-hub-rules/blob/main/pii-patterns.md#L93

**금지 케이스:**
- 진료 기록 ID를 클라이언트 URL/API에 노출 (예: `/medical-records/CHR-2026-000001`)
- 진료 기록 ID 기반 환자 추적 가능하도록 검색 엔드포인트 노출
- 진료 기록 메타데이터(생성일, 의사명, 진단명)를 ID와 함께 반환

**우회 방법:**
- DB 레이어: 내부 시스템 ID만 사용, 의료 기록 ID는 암호화 저장
- API: 권한 검증 후 필요한 필드만 마스킹 반환
- URL: 불투명한 토큰 기반 접근 (예: `/medical-records/{token}` → 서버에서 복호화)

---

## 종합 원칙

**AI 코드 작성 시 체크리스트:**

- [ ] 위 5종 PII 패턴이 코드에 평문으로 노출되지 않았나?
- [ ] 로그, API 응답, 에러 메시지에서 PII가 드러나지 않았나?
- [ ] 암호화/해시/마스킹 처리가 DB 레이어에서만 아니라 애플리케이션 로직에도 적용되었나?
- [ ] 외부 API(SMS, 이메일, 문서 생성)에 위임할 때 필요한 정보만 전달하는가?
- [ ] 감사 로그(audit logs)에 PII가 포함되지 않았나?

**Exception 허용 불가:**
이 5가지 패턴에 대한 예외 조항(exception)은 절대 허용되지 않습니다. 모든 PII는 Layer E (사용자 리뷰) 단계에서도 거절됩니다.
